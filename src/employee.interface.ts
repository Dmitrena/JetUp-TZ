export interface IEmployee {
  firstName: string;
  position: string;
  description: string;
}

export interface IEmployeeRow {
  id: number;
  first_name: string;
  position: string;
  description: string;
}
